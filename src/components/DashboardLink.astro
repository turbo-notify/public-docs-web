---
interface Props {
  path: string;
}

const { path } = Astro.props;
const dashboardUrl = `https://dashboard.turbonotify.com${path}`;
---

<a href={dashboardUrl} target="_blank" rel="noopener noreferrer">
  <slot /><span class="sr-only"> (abre em nova aba)</span>
</a>
