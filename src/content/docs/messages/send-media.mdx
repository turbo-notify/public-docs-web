---
title: Envio de Mídia
description: Como enviar imagens, áudios, vídeos, documentos e figurinhas pelo Turbo Notify.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ApiResponse from '@components/ApiResponse.astro';
import DashboardLink from '@components/DashboardLink.astro';

Mídia (imagens, áudios, vídeos, documentos e figurinhas) é enviada
pelo mesmo endpoint `POST /v1/messages` usado para texto. O corpo é
**plano**: os campos de mídia ficam no nível raiz junto de
`content_type` — não há objeto `content` aninhado.

:::caution[Implementação em progresso]
O contrato HTTP é final, mas o upload outbound de mídia para o
WhatsApp está em finalização (gateway neonize). Requests para os 5
tipos de mídia abaixo são aceitos pela API e geram `message_id`, mas
o worker pode retornar `message.send.failed` com `whatsapp_send_failed`
até o release que completa o gateway. Acompanhe o webhook
`message.sent` para confirmar o envio.
:::

## Tipos de mídia suportados

| `content_type` | Formatos comuns | Tamanho máximo | `caption`? | `filename`? |
| -------------- | --------------- | :------------: | :--------: | :---------: |
| `image` | JPEG, PNG, WebP | 16 MB | ✓ | — |
| `video` | MP4 | 100 MB | ✓ | — |
| `audio` | MP3, OGG, M4A | 16 MB | — | — |
| `document` | PDF, DOCX, XLSX, etc. | 100 MB | ✓ | ✓ |
| `sticker` | WebP animado | 500 KB | — | — |

`caption` é descartado server-side em `audio` e `sticker` — o WhatsApp
ignora o campo nesses tipos.

## Modalidade — URL pública

Forneça uma URL HTTPS acessível publicamente. O Turbo Notify busca o
arquivo antes de entregar ao WhatsApp.

### Imagem

<Tabs>
  <TabItem label="cURL">
    ```bash title="send-image.sh"
    curl -X POST \
      https://api.turbonotify.com/v1/messages \
      -H "Authorization: Bearer SUA_CHAVE" \
      -H "Content-Type: application/json" \
      -d '{
        "to": "+5511999999999",
        "content_type": "image",
        "url": "https://cdn.exemplo.com/foto-produto.jpg",
        "mime_type": "image/jpeg",
        "caption": "Foto do produto"
      }'
    ```
  </TabItem>
  <TabItem label="Node">
    ```js title="send-image.js"
    await fetch('https://api.turbonotify.com/v1/messages', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer SUA_CHAVE',
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        to: '+5511999999999',
        content_type: 'image',
        url: 'https://cdn.exemplo.com/foto-produto.jpg',
        mime_type: 'image/jpeg',
        caption: 'Foto do produto'
      })
    });
    ```
  </TabItem>
  <TabItem label="Python">
    ```python title="send_image.py"
    import requests

    requests.post(
        "https://api.turbonotify.com/v1/messages",
        headers={
            "Authorization": "Bearer SUA_CHAVE",
            "Content-Type": "application/json",
        },
        json={
            "to": "+5511999999999",
            "content_type": "image",
            "url": "https://cdn.exemplo.com/foto-produto.jpg",
            "mime_type": "image/jpeg",
            "caption": "Foto do produto",
        },
    )
    ```
  </TabItem>
</Tabs>

### Vídeo

<Tabs>
  <TabItem label="cURL">
    ```bash title="send-video.sh"
    curl -X POST \
      https://api.turbonotify.com/v1/messages \
      -H "Authorization: Bearer SUA_CHAVE" \
      -H "Content-Type: application/json" \
      -d '{
        "to": "+5511999999999",
        "content_type": "video",
        "url": "https://cdn.exemplo.com/clip-produto.mp4",
        "mime_type": "video/mp4",
        "caption": "Veja o vídeo"
      }'
    ```
  </TabItem>
  <TabItem label="Node">
    ```js title="send-video.js"
    await fetch('https://api.turbonotify.com/v1/messages', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer SUA_CHAVE',
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        to: '+5511999999999',
        content_type: 'video',
        url: 'https://cdn.exemplo.com/clip-produto.mp4',
        mime_type: 'video/mp4',
        caption: 'Veja o vídeo'
      })
    });
    ```
  </TabItem>
  <TabItem label="Python">
    ```python title="send_video.py"
    import requests

    requests.post(
        "https://api.turbonotify.com/v1/messages",
        headers={
            "Authorization": "Bearer SUA_CHAVE",
            "Content-Type": "application/json",
        },
        json={
            "to": "+5511999999999",
            "content_type": "video",
            "url": "https://cdn.exemplo.com/clip-produto.mp4",
            "mime_type": "video/mp4",
            "caption": "Veja o vídeo",
        },
    )
    ```
  </TabItem>
</Tabs>

### Áudio

<Tabs>
  <TabItem label="cURL">
    ```bash title="send-audio.sh"
    curl -X POST \
      https://api.turbonotify.com/v1/messages \
      -H "Authorization: Bearer SUA_CHAVE" \
      -H "Content-Type: application/json" \
      -d '{
        "to": "+5511999999999",
        "content_type": "audio",
        "url": "https://cdn.exemplo.com/voice-note.ogg",
        "mime_type": "audio/ogg"
      }'
    ```
  </TabItem>
  <TabItem label="Node">
    ```js title="send-audio.js"
    await fetch('https://api.turbonotify.com/v1/messages', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer SUA_CHAVE',
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        to: '+5511999999999',
        content_type: 'audio',
        url: 'https://cdn.exemplo.com/voice-note.ogg',
        mime_type: 'audio/ogg'
      })
    });
    ```
  </TabItem>
  <TabItem label="Python">
    ```python title="send_audio.py"
    import requests

    requests.post(
        "https://api.turbonotify.com/v1/messages",
        headers={
            "Authorization": "Bearer SUA_CHAVE",
            "Content-Type": "application/json",
        },
        json={
            "to": "+5511999999999",
            "content_type": "audio",
            "url": "https://cdn.exemplo.com/voice-note.ogg",
            "mime_type": "audio/ogg",
        },
    )
    ```
  </TabItem>
</Tabs>

`mime_type` para voice notes geralmente é `audio/ogg`; para músicas
use `audio/mpeg` ou `audio/mp4`.

### Documento

<Tabs>
  <TabItem label="cURL">
    ```bash title="send-document.sh"
    curl -X POST \
      https://api.turbonotify.com/v1/messages \
      -H "Authorization: Bearer SUA_CHAVE" \
      -H "Content-Type: application/json" \
      -d '{
        "to": "+5511999999999",
        "content_type": "document",
        "url": "https://cdn.exemplo.com/fatura.pdf",
        "mime_type": "application/pdf",
        "filename": "Fatura-Abril.pdf",
        "caption": "Sua fatura do mês de abril"
      }'
    ```
  </TabItem>
  <TabItem label="Node">
    ```js title="send-document.js"
    await fetch('https://api.turbonotify.com/v1/messages', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer SUA_CHAVE',
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        to: '+5511999999999',
        content_type: 'document',
        url: 'https://cdn.exemplo.com/fatura.pdf',
        mime_type: 'application/pdf',
        filename: 'Fatura-Abril.pdf',
        caption: 'Sua fatura do mês de abril'
      })
    });
    ```
  </TabItem>
  <TabItem label="Python">
    ```python title="send_document.py"
    import requests

    requests.post(
        "https://api.turbonotify.com/v1/messages",
        headers={
            "Authorization": "Bearer SUA_CHAVE",
            "Content-Type": "application/json",
        },
        json={
            "to": "+5511999999999",
            "content_type": "document",
            "url": "https://cdn.exemplo.com/fatura.pdf",
            "mime_type": "application/pdf",
            "filename": "Fatura-Abril.pdf",
            "caption": "Sua fatura do mês de abril",
        },
    )
    ```
  </TabItem>
</Tabs>

`filename` aparece como o nome do anexo no chat do destinatário.

### Figurinha (sticker)

<Tabs>
  <TabItem label="cURL">
    ```bash title="send-sticker.sh"
    curl -X POST \
      https://api.turbonotify.com/v1/messages \
      -H "Authorization: Bearer SUA_CHAVE" \
      -H "Content-Type: application/json" \
      -d '{
        "to": "+5511999999999",
        "content_type": "sticker",
        "url": "https://cdn.exemplo.com/sticker-animado.webp",
        "mime_type": "image/webp"
      }'
    ```
  </TabItem>
  <TabItem label="Node">
    ```js title="send-sticker.js"
    await fetch('https://api.turbonotify.com/v1/messages', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer SUA_CHAVE',
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        to: '+5511999999999',
        content_type: 'sticker',
        url: 'https://cdn.exemplo.com/sticker-animado.webp',
        mime_type: 'image/webp'
      })
    });
    ```
  </TabItem>
  <TabItem label="Python">
    ```python title="send_sticker.py"
    import requests

    requests.post(
        "https://api.turbonotify.com/v1/messages",
        headers={
            "Authorization": "Bearer SUA_CHAVE",
            "Content-Type": "application/json",
        },
        json={
            "to": "+5511999999999",
            "content_type": "sticker",
            "url": "https://cdn.exemplo.com/sticker-animado.webp",
            "mime_type": "image/webp",
        },
    )
    ```
  </TabItem>
</Tabs>

Stickers devem ser WebP animados (formato exigido pelo WhatsApp) e
respeitar o limite de 500 KB.

## Modalidade — objeto no seu bucket (BYO Storage)

Se você já possui o arquivo no seu BYO Object Storage, informe `s3_object_key`
em vez de `url`. O Turbo Notify acessa o arquivo diretamente usando as
credenciais que você configurou no Dashboard.

:::caution[BYO Storage obrigatório]
O campo `s3_object_key` exige que o <DashboardLink path="/settings/integrations/storage">Object Storage</DashboardLink>
esteja configurado e ativo. Sem integração ativa, a requisição retorna
`412 Precondition Failed` com código `storage_required`.
:::

```json title="Exemplo — imagem por s3_object_key"
{
  "to": "+5511999999999",
  "content_type": "image",
  "s3_object_key": "media/fotos/produto-42.jpg",
  "mime_type": "image/jpeg",
  "caption": "Foto do produto"
}
```

### Erro 412 — storage_required

Quando `s3_object_key` é informado mas nenhum Object Storage está configurado:

```json title="412 storage_required"
{
  "error": "storage_required",
  "message": "BYO Object Storage is required to use s3_object_key. Configure it in the Dashboard.",
  "hint": {
    "setup_url": "https://app.turbonotify.com/settings/integrations/storage",
    "docs_url": "https://docs.turbonotify.com/general/byo-storage-setup/"
  }
}
```

## Esquema de campos (todos os tipos)

| Campo | Tipo | Obrigatório | Notas |
|---|---|---|---|
| `content_type` | enum | sim | `image` / `video` / `audio` / `document` / `sticker` |
| `url` | URL | condicional | URL pública HTTPS; exclusivo com `s3_object_key` |
| `s3_object_key` | string | condicional | Chave do objeto no seu BYO Storage; exclusivo com `url`. Exige storage configurado. |
| `mime_type` | string | não | recomendado; o worker pode inferir do `Content-Type` da resposta |
| `caption` | string | parcial | aceito em `image`, `video`, `document`; **descartado** em `audio` e `sticker` |
| `filename` | string | não | apenas em `document`; aparece como nome do anexo no chat |

## Resposta

```json title="response.json"
{
  "message_id": "msg_a1b2c3d4e5f60718293a4b5c6d7e8f90",
  "status": {
    "state": "pending",
    "at": "2026-04-27T10:00:05.123456+00:00",
    "reason": null
  }
}
```

A confirmação real chega via webhook `message.sent`. Em caso de falha
(URL inacessível, formato não suportado, tamanho excedido) você recebe
`message.send.failed` com `error_code` específico.

## Webhook outbound

Quando o envio é confirmado pelo WhatsApp, o webhook `message.sent`
carrega `content_type` e `preview` (mesmo shape de `message.received`):

```json title="webhook-message-sent.json"
{
  "id": "evt_abc123def456789012345678",
  "type": "message.sent",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "created_at": "2026-04-27T10:00:08Z",
  "data": {
    "id": "msg_a1b2c3d4e5f60718293a4b5c6d7e8f90",
    "inbox": { "number": "5511999990001", "alias": "main" },
    "from": { "number": "5511999990001", "alias": "main" },
    "to":   { "number": "5511999999999" },
    "preview": "Foto do produto",
    "content_type": "image",
    "direction": "outbound",
    "channel": "whatsapp",
    "origin": "tn",
    "at": "2026-04-27T10:00:08Z"
  }
}
```

Para baixar o arquivo de uma mídia recebida, consulte
[`GET /v1/messages/{id}`](/messages/get/) — o body inclui `media.url`
com URL pré-assinada do seu BYO Object Storage (quando configurado).

## Relacionado

- [Envio de mensagens](/messages/send/) — texto, localização, contato, calendário (mesmo endpoint).
- [Webhook outbound](/messages/webhook/) — payloads de `message.sent` e `message.delivered`.
- [Recuperar mensagem](/messages/get/) — obter o body completo, inclusive mídia.
