---
title: Responder Mensagens
description: Envie mensagens que citam uma mensagem anterior, criando encadeamento visual no WhatsApp.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ApiResponse from '@components/ApiResponse.astro';

Responda mensagens recebidas criando um encadeamento visual no WhatsApp. A resposta aparece vinculada à mensagem original, facilitando o acompanhamento da conversa pelo destinatário.

## Enviar resposta

Para responder uma mensagem, faça uma requisição `POST` informando o `message_id` da mensagem original na URL e o texto da resposta no corpo.

<Tabs>
  <TabItem label="cURL">
    ```bash title="turbo-notify.sh"
    curl -X POST \
      https://api.turbonotify.com/messages/msg_abc123/reply \
      -H "Authorization: Bearer SUA_CHAVE" \
      -H "Content-Type: application/json" \
      -d '{
        "text": "Obrigado pela sua mensagem!"
      }'
    ```
  </TabItem>
  <TabItem label="Node">
    ```js title="turbo-notify.js"
    const response = await fetch(
      'https://api.turbonotify.com/messages/msg_abc123/reply',
      {
        method: 'POST',
        headers: {
          'Authorization': 'Bearer SUA_CHAVE',
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({
          text: 'Obrigado pela sua mensagem!'
        })
      }
    );

    const result = await response.json();
    console.log(result);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python title="turbo_notify.py"
    import requests

    url = "https://api.turbonotify.com/messages/msg_abc123/reply"
    headers = {
        "Authorization": "Bearer SUA_CHAVE",
        "Content-Type": "application/json"
    }
    data = {
        "text": "Obrigado pela sua mensagem!"
    }

    response = requests.post(url, json=data, headers=headers)
    result = response.json()
    print(result)
    ```
  </TabItem>
  <TabItem label="Go">
    ```go title="turbo_notify.go"
    package main

    import (
        "fmt"
        "io"
        "net/http"
        "strings"
    )

    func main() {
        payload := strings.NewReader(`{"text": "Obrigado pela sua mensagem!"}`)
        req, _ := http.NewRequest("POST", "https://api.turbonotify.com/messages/msg_abc123/reply", payload)
        req.Header.Set("Authorization", "Bearer SUA_CHAVE")
        req.Header.Set("Content-Type", "application/json")
        resp, _ := http.DefaultClient.Do(req)
        defer resp.Body.Close()
        body, _ := io.ReadAll(resp.Body)
        fmt.Println(string(body))
    }
    ```
  </TabItem>
  <TabItem label="PHP">
    ```php title="turbo-notify.php"
    <?php
    $url = 'https://api.turbonotify.com/messages/msg_abc123/reply';
    $data = [
        'text' => 'Obrigado pela sua mensagem!'
    ];

    $options = [
        'http' => [
            'header' => [
                'Authorization: Bearer SUA_CHAVE',
                'Content-Type: application/json'
            ],
            'method' => 'POST',
            'content' => json_encode($data)
        ]
    ];

    $context = stream_context_create($options);
    $result = file_get_contents($url, false, $context);
    $response = json_decode($result, true);
    var_dump($response);
    ?>
    ```
  </TabItem>
  <TabItem label="C#">
    ```csharp title="TurboNotify.cs"
    using System.Text;
    using System.Text.Json;

    var client = new HttpClient();
    client.DefaultRequestHeaders.Add("Authorization", "Bearer SUA_CHAVE");

    var data = new {
        text = "Obrigado pela sua mensagem!"
    };

    var json = JsonSerializer.Serialize(data);
    var content = new StringContent(json, Encoding.UTF8, "application/json");

    var response = await client.PostAsync(
        "https://api.turbonotify.com/messages/msg_abc123/reply",
        content
    );
    var result = await response.Content.ReadAsStringAsync();
    Console.WriteLine(result);
    ```
  </TabItem>
  <TabItem label="Java">
    ```java title="TurboNotify.java"
    import java.net.http.*;
    import java.net.URI;

    public class TurboNotify {
        public static void main(String[] args) throws Exception {
            var client = HttpClient.newHttpClient();

            var json = """
                {
                    "text": "Obrigado pela sua mensagem!"
                }
                """;

            var request = HttpRequest.newBuilder()
                .uri(URI.create("https://api.turbonotify.com/messages/msg_abc123/reply"))
                .header("Authorization", "Bearer SUA_CHAVE")
                .header("Content-Type", "application/json")
                .POST(HttpRequest.BodyPublishers.ofString(json))
                .build();

            var response = client.send(request,
                HttpResponse.BodyHandlers.ofString());
            System.out.println(response.body());
        }
    }
    ```
  </TabItem>
</Tabs>

<ApiResponse
  status={202}
  body={{
    message_id: 'msg_def456',
    operation_id: 'op_msg_002',
    status: { state: 'pending', at: '2025-07-16T10:05:00Z' },
  }}
/>

## Resposta com número extra

Para responder usando um número extra, informe o campo `from` com o número cadastrado e ativo na sua conta.

<Tabs>
  <TabItem label="cURL">
    ```bash title="turbo-notify.sh"
    curl -X POST \
      https://api.turbonotify.com/messages/msg_abc123/reply \
      -H "Authorization: Bearer SUA_CHAVE" \
      -H "Content-Type: application/json" \
      -d '{
        "from": {"alias": "vendas-sp"},
        "text": "Obrigado pela sua mensagem!"
      }'
    ```
  </TabItem>
  <TabItem label="Node">
    ```js title="turbo-notify.js"
    const response = await fetch(
      'https://api.turbonotify.com/messages/msg_abc123/reply',
      {
        method: 'POST',
        headers: {
          'Authorization': 'Bearer SUA_CHAVE',
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({
          from: { alias: 'vendas-sp' },
          text: 'Obrigado pela sua mensagem!'
        })
      }
    );

    const result = await response.json();
    console.log(result);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python title="turbo_notify.py"
    import requests

    url = "https://api.turbonotify.com/messages/msg_abc123/reply"
    headers = {
        "Authorization": "Bearer SUA_CHAVE",
        "Content-Type": "application/json"
    }
    data = {
        "from": {"alias": "vendas-sp"},
        "text": "Obrigado pela sua mensagem!"
    }

    response = requests.post(url, json=data, headers=headers)
    result = response.json()
    print(result)
    ```
  </TabItem>
  <TabItem label="Go">
    ```go title="turbo_notify.go"
    package main

    import (
        "fmt"
        "io"
        "net/http"
        "strings"
    )

    func main() {
        payload := strings.NewReader(`{"from": {"alias": "vendas-sp"}, "text": "Obrigado pela sua mensagem!"}`)
        req, _ := http.NewRequest("POST", "https://api.turbonotify.com/messages/msg_abc123/reply", payload)
        req.Header.Set("Authorization", "Bearer SUA_CHAVE")
        req.Header.Set("Content-Type", "application/json")
        resp, _ := http.DefaultClient.Do(req)
        defer resp.Body.Close()
        body, _ := io.ReadAll(resp.Body)
        fmt.Println(string(body))
    }
    ```
  </TabItem>
  <TabItem label="PHP">
    ```php title="turbo-notify.php"
    <?php
    $url = 'https://api.turbonotify.com/messages/msg_abc123/reply';
    $data = [
        'from' => ['alias' => 'vendas-sp'],
        'text' => 'Obrigado pela sua mensagem!'
    ];

    $options = [
        'http' => [
            'header' => [
                'Authorization: Bearer SUA_CHAVE',
                'Content-Type: application/json'
            ],
            'method' => 'POST',
            'content' => json_encode($data)
        ]
    ];

    $context = stream_context_create($options);
    $result = file_get_contents($url, false, $context);
    $response = json_decode($result, true);
    var_dump($response);
    ?>
    ```
  </TabItem>
  <TabItem label="C#">
    ```csharp title="TurboNotify.cs"
    using System.Text;
    using System.Text.Json;

    var client = new HttpClient();
    client.DefaultRequestHeaders.Add("Authorization", "Bearer SUA_CHAVE");

    var data = new {
        from = new { alias = "vendas-sp" },
        text = "Obrigado pela sua mensagem!"
    };

    var json = JsonSerializer.Serialize(data);
    var content = new StringContent(json, Encoding.UTF8, "application/json");

    var response = await client.PostAsync(
        "https://api.turbonotify.com/messages/msg_abc123/reply",
        content
    );
    var result = await response.Content.ReadAsStringAsync();
    Console.WriteLine(result);
    ```
  </TabItem>
  <TabItem label="Java">
    ```java title="TurboNotify.java"
    import java.net.http.*;
    import java.net.URI;

    public class TurboNotify {
        public static void main(String[] args) throws Exception {
            var client = HttpClient.newHttpClient();

            var json = """
                {
                    "from": {"alias": "vendas-sp"},
                    "text": "Obrigado pela sua mensagem!"
                }
                """;

            var request = HttpRequest.newBuilder()
                .uri(URI.create("https://api.turbonotify.com/messages/msg_abc123/reply"))
                .header("Authorization", "Bearer SUA_CHAVE")
                .header("Content-Type", "application/json")
                .POST(HttpRequest.BodyPublishers.ofString(json))
                .build();

            var response = client.send(request,
                HttpResponse.BodyHandlers.ofString());
            System.out.println(response.body());
        }
    }
    ```
  </TabItem>
</Tabs>

Também é possível usar o alias definido no cadastro do número extra no campo `from`, em vez do número completo.

## Idempotência

Para evitar respostas duplicadas em caso de falhas de rede ou retentativas, utilize o cabeçalho `Idempotency-Key` com um valor único por requisição.

<Tabs>
  <TabItem label="cURL">
    ```bash title="turbo-notify.sh"
    curl -X POST \
      https://api.turbonotify.com/messages/msg_abc123/reply \
      -H "Authorization: Bearer SUA_CHAVE" \
      -H "Content-Type: application/json" \
      -H "Idempotency-Key: chave-unica-123" \
      -d '{
        "text": "Obrigado pela sua mensagem!"
      }'
    ```
  </TabItem>
  <TabItem label="Node">
    ```js title="turbo-notify.js"
    const response = await fetch(
      'https://api.turbonotify.com/messages/msg_abc123/reply',
      {
        method: 'POST',
        headers: {
          'Authorization': 'Bearer SUA_CHAVE',
          'Content-Type': 'application/json',
          'Idempotency-Key': 'chave-unica-123'
        },
        body: JSON.stringify({
          text: 'Obrigado pela sua mensagem!'
        })
      }
    );

    const result = await response.json();
    console.log(result);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python title="turbo_notify.py"
    import requests

    url = "https://api.turbonotify.com/messages/msg_abc123/reply"
    headers = {
        "Authorization": "Bearer SUA_CHAVE",
        "Content-Type": "application/json",
        "Idempotency-Key": "chave-unica-123"
    }
    data = {
        "text": "Obrigado pela sua mensagem!"
    }

    response = requests.post(url, json=data, headers=headers)
    result = response.json()
    print(result)
    ```
  </TabItem>
  <TabItem label="Go">
    ```go title="turbo_notify.go"
    package main

    import (
        "fmt"
        "io"
        "net/http"
        "strings"
    )

    func main() {
        payload := strings.NewReader(`{"text": "Obrigado pela sua mensagem!"}`)
        req, _ := http.NewRequest("POST", "https://api.turbonotify.com/messages/msg_abc123/reply", payload)
        req.Header.Set("Authorization", "Bearer SUA_CHAVE")
        req.Header.Set("Content-Type", "application/json")
        req.Header.Set("Idempotency-Key", "chave-unica-123")
        resp, _ := http.DefaultClient.Do(req)
        defer resp.Body.Close()
        body, _ := io.ReadAll(resp.Body)
        fmt.Println(string(body))
    }
    ```
  </TabItem>
  <TabItem label="PHP">
    ```php title="turbo-notify.php"
    <?php
    $url = 'https://api.turbonotify.com/messages/msg_abc123/reply';
    $data = [
        'text' => 'Obrigado pela sua mensagem!'
    ];

    $options = [
        'http' => [
            'header' => [
                'Authorization: Bearer SUA_CHAVE',
                'Content-Type: application/json',
                'Idempotency-Key: chave-unica-123'
            ],
            'method' => 'POST',
            'content' => json_encode($data)
        ]
    ];

    $context = stream_context_create($options);
    $result = file_get_contents($url, false, $context);
    $response = json_decode($result, true);
    var_dump($response);
    ?>
    ```
  </TabItem>
  <TabItem label="C#">
    ```csharp title="TurboNotify.cs"
    using System.Text;
    using System.Text.Json;

    var client = new HttpClient();
    client.DefaultRequestHeaders.Add("Authorization", "Bearer SUA_CHAVE");
    client.DefaultRequestHeaders.Add("Idempotency-Key", "chave-unica-123");

    var data = new {
        text = "Obrigado pela sua mensagem!"
    };

    var json = JsonSerializer.Serialize(data);
    var content = new StringContent(json, Encoding.UTF8, "application/json");

    var response = await client.PostAsync(
        "https://api.turbonotify.com/messages/msg_abc123/reply",
        content
    );
    var result = await response.Content.ReadAsStringAsync();
    Console.WriteLine(result);
    ```
  </TabItem>
  <TabItem label="Java">
    ```java title="TurboNotify.java"
    import java.net.http.*;
    import java.net.URI;

    public class TurboNotify {
        public static void main(String[] args) throws Exception {
            var client = HttpClient.newHttpClient();

            var json = """
                {
                    "text": "Obrigado pela sua mensagem!"
                }
                """;

            var request = HttpRequest.newBuilder()
                .uri(URI.create("https://api.turbonotify.com/messages/msg_abc123/reply"))
                .header("Authorization", "Bearer SUA_CHAVE")
                .header("Content-Type", "application/json")
                .header("Idempotency-Key", "chave-unica-123")
                .POST(HttpRequest.BodyPublishers.ofString(json))
                .build();

            var response = client.send(request,
                HttpResponse.BodyHandlers.ofString());
            System.out.println(response.body());
        }
    }
    ```
  </TabItem>
</Tabs>

Se a mesma `Idempotency-Key` for enviada novamente dentro do período de retenção, a API retornará a resposta original sem processar a requisição novamente.

## Acompanhamento

A resposta segue o mesmo fluxo de status de uma mensagem comum. Acompanhe o andamento pela API ou via webhook. Veja mais em [Status](/messages/status/).

## Erros possíveis

| Status | Código de erro            | Descrição                                                       |
| :----: | ------------------------- | --------------------------------------------------------------- |
| `400`  | `invalid_json`            | O JSON da requisição está malformado ou inválido.               |
| `403`  | `not_authorized`          | A chave de acesso está inválida ou ausente.                     |
| `422`  | `message_not_found`       | A mensagem original não foi encontrada ou já expirou.           |
| `422`  | `sender_not_authorized`   | O campo `from` não está ativo na conta.                         |
| `422`  | `reply_not_allowed`       | A mensagem original não permite resposta.                       |
| `500`  | `internal_error`          | Erro interno inesperado no servidor.                            |
| `503`  | `whatsapp_unavailable`    | O WhatsApp está temporariamente fora do ar.                     |
