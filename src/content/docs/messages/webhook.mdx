---
title: Eventos via webhook
description: Como receber eventos do Turbo Notify via webhook (HTTP push) — mesmos dados do endpoint GET /events.
---

import DashboardLink from '@components/DashboardLink.astro';
import ExternalLink from '@components/ExternalLink.astro';

O Turbo Notify notifica seu sistema sobre eventos — envio confirmado, entrega, leitura, mensagem recebida, reação, falha — via webhook (HTTP POST). O objeto `data` de cada evento é **idêntico** ao retornado pelo endpoint [`GET /events`](/messages/polling/).

## Recebimento dos eventos

Para começar a receber eventos, cadastre a URL do seu webhook no <DashboardLink path="/webhook">Dashboard</DashboardLink>. A URL deve ser pública, acessível via HTTPS e responder com status `2xx` dentro de 5 segundos.

:::tip[Inspecione e aprenda em tempo real]
Antes de plugar o webhook na sua aplicação, abra o <ExternalLink href="https://webhook.turbonotify.com">Webhook Inspector</ExternalLink> e cadastre a URL gerada. O Inspector recebe cada delivery em tempo real (via WebSocket), exibe headers/body completos, valida a assinatura HMAC interativamente e sugere a próxima chamada REST típica para cada tipo de evento (`message.received` → `GET /v1/messages/{id}`).
:::

## Privacidade

Recomendamos que sua URL de webhook não exponha informações sensíveis, como chaves de acesso ou identificadores internos. Use caminhos genéricos e valide a autenticidade da requisição pelo cabeçalho de assinatura.

## Segurança

Toda requisição de webhook enviada pelo Turbo Notify inclui os cabeçalhos abaixo. Utilize a assinatura para garantir que a requisição foi realmente enviada pelo Turbo Notify e não por terceiros.

| Header | Descrição |
|--------|-----------|
| `X-Webhook-Signature` | `sha256=<hex>` — HMAC-SHA256 de `{X-Webhook-Timestamp}.{raw_body}` com seu segredo. |
| `X-Webhook-Timestamp` | Unix epoch (segundos) — utilize para rejeitar deliveries com mais de ~5 minutos (proteção contra replay). |
| `X-Webhook-Attempt` | Número da tentativa atual (começa em 1, incrementa nos retries). |
| `X-Webhook-Max-Attempts` | Total de tentativas que o Turbo Notify fará antes de desistir. |
| `User-Agent` | `TurboNotify-Webhook/1.0` — útil para WAF allowlists. |

O segredo para validação está disponível no <DashboardLink path="/webhook">Dashboard</DashboardLink>, na mesma seção onde você cadastra a URL.

Exemplo de validação da assinatura:

```python title="validacao.py"
import hmac
import hashlib

def verificar_assinatura(payload: bytes, timestamp: str, assinatura: str, segredo: str) -> bool:
    dados_assinados = f"{timestamp}.".encode() + payload
    esperado = "sha256=" + hmac.new(
        segredo.encode(),
        dados_assinados,
        hashlib.sha256
    ).hexdigest()
    return hmac.compare_digest(esperado, assinatura)

# Uso:
# timestamp = request.headers["X-Webhook-Timestamp"]
# assinatura = request.headers["X-Webhook-Signature"]
# verificar_assinatura(request.body, timestamp, assinatura, segredo)
```

```js title="validacao.js"
const crypto = require('crypto');

function verificarAssinatura(payload, timestamp, assinatura, segredo) {
  const dadosAssinados = `${timestamp}.${payload}`;
  const esperado = 'sha256=' + crypto
    .createHmac('sha256', segredo)
    .update(dadosAssinados)
    .digest('hex');
  return crypto.timingSafeEqual(
    Buffer.from(esperado),
    Buffer.from(assinatura)
  );
}

// Uso:
// const timestamp = req.headers['x-webhook-timestamp'];
// const assinatura = req.headers['x-webhook-signature'];
// verificarAssinatura(req.body, timestamp, assinatura, segredo);
```

## Estrutura do evento

Toda requisição de webhook segue a mesma estrutura base:

| Propriedade       | Tipo     | Descrição                                                                 |
| ----------------- | -------- | ------------------------------------------------------------------------- |
| `id`              | `string` | Identificador único do evento (`evt_…`). Use como chave de idempotência.  |
| `type`            | `string` | Tipo do evento (ex.: `message.sent`).                                     |
| `organization_id` | `string` | Identificador público da sua organização (`org_{hex32}`). Sempre presente.|
| `number_alias`    | `string` | Handle do número Turbo Notify ("inbox") ao qual o evento pertence — `"main"` para o número principal ou o alias de um número extra. É a **string do alias**, nunca um UUID. Presente em eventos ligados a um número específico — `message.*`, `number.*`, `group.*` e o ciclo de vida de `contact.*` (a observação do contato é atribuída ao número cujo inbox a viu); **omitido** apenas em eventos de escopo de conta sem número único — `storage.*` e os de quota (incluindo `contact.quota.*`). |
| `created_at`      | `string` | Momento de **emissão** do evento pelo TN — distinto de `data.at` (momento da ação no WhatsApp). ISO 8601 com offset do timezone da conta (ex.: `2026-06-01T10:00:05-03:00`). Fallback para `+00:00` quando nenhum timezone está configurado. |
| `data`            | `object` | Dados específicos do evento (veja [Campos da mensagem](#campos-da-mensagem)). |

:::note[Receipts e direção]
Os eventos `message.delivered` e `message.read` representam **confirmações de leitura/entrega**. `direction` é sempre `"inbound"` (a confirmação chega do destinatário). O recibo é modelado como um evento inbound:

- **`from`** = quem **disparou o recibo** (o contato que entregou/leu). Formato de pessoa, **sem `alias`**.
- **`to`** = o **destinatário do recibo**: em conversas diretas, o seu número (com `alias`); em grupos, o **grupo** (`group_id` + `name`).

A linha Turbo Notify que recebeu o recibo é identificada pelo `number_alias` do envelope; resolva seu número/contato via `GET /v1/numbers/{alias}`.

Em **grupos**, cada outro participante gera o seu próprio par `delivered`/`read`, sempre com `from` = aquele participante e `to` = o grupo. Não confunda com `message.received`, em que `direction="inbound"` reflete uma mensagem nova vinda do contato.
:::

## Campos da mensagem

Todos os eventos `message.*` compartilham os campos abaixo em `data`:

| Campo | Tipo | Descrição |
|-------|------|-----------|
| `id` | `string \| null` | Identificador público da mensagem (`msg_{hex32}`). `null` apenas em `message.send.failed` (row não criada). |
| `from` | `object` | Quem originou o evento. Pessoa (inbound): `{contact_id?, name?, number}`. Outbound: `{contact_id?, name?, number, alias}`. |
| `to` | `object` | Destinatário. 1:1: `{contact_id?, name?, number}`. Grupo: `{group_id, name?}`. |
| `preview` | `string` | Recorte do conteúdo (até 50 chars). Veja [Preview por tipo](#preview-por-tipo). |
| `content_type` | `string` | `text`, `image`, `audio`, `video`, `document`, `sticker`, `location`, `calendar`, `poll`. Mensagens de tipo desconhecido chegam como `unsupported`. |
| `direction` | `string` | `"outbound"` (ação do tenant/API) ou `"inbound"` (ação do peer/WhatsApp). |
| `channel` | `string` | Sempre `"whatsapp"`. |
| `origin` | `string` | `"tn"` (ação originada via API do Turbo Notify) ou `"native"` (originada no canal WhatsApp). Eventos inbound (`message.received`/`delivered`/`read`) são sempre `"native"`. Sempre presente. |
| `at` | `string` | ISO 8601 com offset — momento WhatsApp do evento (envio, entrega, leitura, edição, etc.). |

> **Ordem dos campos do objeto party** (`from`/`to`): sempre
> `contact_id, name, number, alias` (alias onde existir; campos opcionais
> omitidos quando `null`). `GroupToParty` é a exceção — mantém `{group_id, name?}`.
> A linha Turbo Notify do evento é identificada pelo `number_alias` do envelope;
> resolva seu número/contato via `GET /v1/numbers/{alias}`.
> `created_at` (envelope) é o momento de **emissão** do evento; `at` (em `data`)
> é o momento da **ação no WhatsApp** — valores distintos.

Eventos `message.*.failed` adicionam um objeto `error` estruturado — **o mesmo
shape** do `status.error` retornado por `GET /v1/messages/{id}` e
`GET /v1/messages/{id}/status` (coerência evento ↔ recurso):

| Campo | Tipo | Descrição |
|-------|------|-----------|
| `error` | `object` | Erro da falha. Presente em todos os eventos `*.failed`. |
| `error.code` | `string` | Código estável legível por máquina — faça branch neste valor (ex.: `media_fetch_failed`, `unsupported_media_format`). |
| `error.message` | `string` | Descrição legível da falha. |
| `error.hint` | `string \| null` | Orientação acionável para corrigir, derivada do `code`. `null` quando não há orientação. |

### Preview por tipo

| `content_type` | Preview |
|---|---|
| `text` | Texto truncado |
| `image` | Legenda ou `[Image]` |
| `sticker` | `[Sticker]` |
| `video` | Legenda ou `[Video]` |
| `audio` | Transcrição Whisper (se configurada) ou `[Audio]` |
| `document` | Legenda → nome do arquivo → `[Document]` |
| `location` | Texto humanizado sem colchetes, ex: `Av. Paulista, Sao Paulo, SP` |
| `calendar` | Sem colchetes, ex: `Reuniao — seg, 2 jun 2026 14:00` |
| `poll` | Nome da enquete truncado |
| `unsupported` | `[Unsupported]` — tipo de mensagem não reconhecido pelo Turbo Notify |

### Exemplo — `message.sent` (outbound)

```json title="webhook-payload.json"
{
  "id": "evt_abc123def456789012345678",
  "type": "message.sent",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T10:00:06-03:00",
  "data": {
    "id": "msg_01993c5a1b24d3e9f01a2b3c4d5e6f70",
    "from": { "name": "Turbo Notify", "number": "5511999990001", "alias": "main" },
    "to": { "name": "João Silva", "number": "5511999990002" },
    "preview": "Sua mensagem...",
    "content_type": "text",
    "direction": "outbound",
    "channel": "whatsapp",
    "origin": "tn",
    "at": "2026-06-01T10:00:05-03:00"
  }
}
```

`created_at` (`10:00:06`, emissão pelo TN) é **distinto** de `data.at`
(`10:00:05`, confirmação de envio no WhatsApp) — o evento foi emitido 1 s após
a ação.

O `number_alias` do envelope identifica a linha Turbo Notify do evento; resolva seu número/contato via `GET /v1/numbers/{alias}`. Em conversas 1:1 outbound essa linha coincide com `from`.

`origin` discrimina a fonte do envio outbound:
- `"tn"` — enviado via API do Turbo Notify.
- `"native"` — detectado diretamente do app WhatsApp (quando o tenant digita pelo celular).

`content_type` e `preview` espelham `message.received` — `text`, `image`, `audio`, `video`, `document`, `sticker`, `location`, `calendar` ou `poll`. Para o body completo, faça `GET /v1/messages/{id}`.

### Exemplo — `message.received` (inbound)

```json title="webhook-payload.json"
{
  "id": "evt_def789abc234567890123456",
  "type": "message.received",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T15:32:11-03:00",
  "data": {
    "id": "msg_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
    "from": { "name": "João Silva", "number": "5511777777777" },
    "to": { "name": "Turbo Notify", "number": "5511999990001", "alias": "main" },
    "preview": "Olá, gostaria de saber...",
    "content_type": "text",
    "direction": "inbound",
    "channel": "whatsapp",
    "origin": "native",
    "at": "2026-06-01T15:32:11-03:00"
  }
}
```

`from.name` traz o nome de exibição configurado no WhatsApp (push name); omitido quando não definido. O `number_alias` do envelope resolve para `"main"` ou o alias do número extra que recebeu.

### Exemplo — `message.received` (grupo)

Disponível desde 2026-05-24. Quando uma mensagem chega de um **grupo WhatsApp**,
`to` carrega `group_id` + snapshot `name` em vez de `number`. A linha Turbo Notify
que participa do grupo é identificada pelo `number_alias` do envelope.
`from` continua sendo o participante que enviou — pessoas sempre falam, nunca grupos.
Para mensagens 1:1 `to.group_id` é omitido — payloads pré-existentes permanecem byte-idênticos.

```json title="message.received (grupo)"
{
  "id": "evt_c00a44c3fc774698b0f130d40bb5f104",
  "type": "message.received",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T22:55:14-03:00",
  "data": {
    "id": "msg_e95500cc03c1492e81e46a77c1f0d420",
    "from": {
      "contact_id": "ct_45f2ece8a66a4eeaa661aeb9b8e58c77",
      "name": "Cleverson",
      "number": "5571999258283"
    },
    "to": {
      "group_id": "grp_e442553a56b14e2695f6acfd7d651b1a",
      "name": "TNT"
    },
    "preview": "Bom dia pessoal",
    "content_type": "text",
    "direction": "inbound",
    "channel": "whatsapp",
    "origin": "native",
    "at": "2026-06-01T22:55:14-03:00"
  }
}
```

- `to.group_id` é o identificador público do grupo (`grp_{hex32}`) — estável por tenant. Nunca expostos JIDs brutos do WhatsApp.
- O `number_alias` do envelope identifica a linha Turbo Notify que participa do grupo. Essencial quando o tenant tem múltiplos números extras — resolva seu número/contato via `GET /v1/numbers/{alias}`.
- Para detectar mensagens de grupo: verifique `"group_id" in data.to` — conversas 1:1 nunca carregam esse campo.

### Exemplo — `message.edited`

```json title="message.edited (inbound — contato editou a mensagem)"
{
  "id": "evt_edited_inbound_001",
  "type": "message.edited",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T11:00:00-03:00",
  "data": {
    "id": "msg_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
    "from": { "name": "João Silva", "number": "5511777777777" },
    "to": { "name": "Turbo Notify", "number": "5511999990001", "alias": "main" },
    "preview": "Texto corrigido da mensagem",
    "content_type": "text",
    "direction": "inbound",
    "channel": "whatsapp",
    "origin": "native",
    "at": "2026-06-01T11:00:00-03:00"
  }
}
```

```json title="message.edited (outbound — você editou a mensagem via API)"
{
  "id": "evt_edited_outbound_001",
  "type": "message.edited",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T11:05:00-03:00",
  "data": {
    "id": "msg_b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5",
    "from": { "name": "Turbo Notify", "number": "5511999990001", "alias": "main" },
    "to": { "name": "João Silva", "number": "5511777777777" },
    "preview": "Texto atualizado da mensagem",
    "content_type": "text",
    "direction": "outbound",
    "channel": "whatsapp",
    "origin": "tn",
    "at": "2026-06-01T11:05:00-03:00"
  }
}
```

### Exemplo — `message.deleted`

```json title="message.deleted (inbound — contato apagou a mensagem)"
{
  "id": "evt_deleted_inbound_001",
  "type": "message.deleted",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T12:00:00-03:00",
  "data": {
    "id": "msg_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
    "from": { "name": "João Silva", "number": "5511777777777" },
    "to": { "name": "Turbo Notify", "number": "5511999990001", "alias": "main" },
    "preview": "",
    "content_type": "text",
    "direction": "inbound",
    "channel": "whatsapp",
    "origin": "native",
    "at": "2026-06-01T12:00:00-03:00"
  }
}
```

```json title="message.deleted (outbound — você apagou a mensagem via API)"
{
  "id": "evt_deleted_outbound_001",
  "type": "message.deleted",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T12:05:00-03:00",
  "data": {
    "id": "msg_b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5",
    "from": { "name": "Turbo Notify", "number": "5511999990001", "alias": "main" },
    "to": { "name": "João Silva", "number": "5511777777777" },
    "preview": "",
    "content_type": "text",
    "direction": "outbound",
    "channel": "whatsapp",
    "origin": "tn",
    "at": "2026-06-01T12:05:00-03:00"
  }
}
```

### Exemplo — `message.reacted`

Para `message.reacted`: `content_type` é sempre `"text"` e `preview` contém o emoji (ou `""` para remoção). Os campos `emoji`, `action` e `reaction_event_id` foram removidos dos eventos — use `GET /v1/messages/{id}` para o detalhe completo da reação.

```json title="message.reacted (inbound — contato reagiu a uma mensagem sua)"
{
  "id": "evt_reacted_inbound_001",
  "type": "message.reacted",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T13:00:00-03:00",
  "data": {
    "id": "msg_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
    "from": { "name": "João Silva", "number": "5511777777777" },
    "to": { "name": "Turbo Notify", "number": "5511999990001", "alias": "main" },
    "preview": "👍",
    "content_type": "text",
    "direction": "inbound",
    "channel": "whatsapp",
    "origin": "native",
    "at": "2026-06-01T13:00:00-03:00"
  }
}
```

```json title="message.reacted (outbound — você reagiu a uma mensagem via API)"
{
  "id": "evt_reacted_outbound_001",
  "type": "message.reacted",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T13:05:00-03:00",
  "data": {
    "id": "msg_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
    "from": { "name": "Turbo Notify", "number": "5511999990001", "alias": "main" },
    "to": { "name": "João Silva", "number": "5511777777777" },
    "preview": "❤️",
    "content_type": "text",
    "direction": "outbound",
    "channel": "whatsapp",
    "origin": "tn",
    "at": "2026-06-01T13:05:00-03:00"
  }
}
```

Para remover uma reação, envie `POST /v1/messages/{id}/react` com `emoji: ""` (string vazia). O evento `message.reacted` é emitido com `preview: ""`.

### Exemplo — `message.replied` (inbound/outbound)

Disponível desde 2026-04-28. Replies são entregues como evento dedicado. `preview` contém o preview da **nova mensagem** (o reply, não a mensagem citada). O `quoted_message_id` foi removido dos eventos — faça `GET /v1/messages/{id}` para acessar `replied.message_id` e `replied.preview`.

```json title="message.replied (inbound)"
{
  "id": "evt_replied_inbound",
  "type": "message.replied",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T10:00:00-03:00",
  "data": {
    "id": "msg_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
    "from": { "name": "João Silva", "number": "5511777777777" },
    "to": { "name": "Turbo Notify", "number": "5511999990001", "alias": "main" },
    "preview": "Sim, é esse pedido mesmo!",
    "content_type": "text",
    "direction": "inbound",
    "channel": "whatsapp",
    "origin": "native",
    "at": "2026-06-01T10:00:00-03:00"
  }
}
```

```json title="message.replied (outbound — enviado via API)"
{
  "id": "evt_replied_outbound",
  "type": "message.replied",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T10:01:00-03:00",
  "data": {
    "id": "msg_b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5",
    "from": { "name": "Turbo Notify", "number": "5511999990001", "alias": "main" },
    "to": { "name": "João Silva", "number": "5511777777777" },
    "preview": "Sem problemas, vou conferir.",
    "content_type": "text",
    "direction": "outbound",
    "channel": "whatsapp",
    "origin": "tn",
    "at": "2026-06-01T10:01:00-03:00"
  }
}
```

Para responder uma mensagem via API, use o endpoint dedicado `POST /v1/messages/{message_id}/reply` (body plano). Veja [/messages/send/](/messages/send/).

### Exemplo — `message.delivered` / `message.read` (receipt inbound)

Receipts são sempre **inbound**. `from` é quem disparou o recibo; `to` identifica o lado tenant (1:1: o seu número; grupo: o grupo). A linha tenant é dada pelo `number_alias` do envelope.

```json title="message.delivered (1:1)"
{
  "id": "evt_delivered_001",
  "type": "message.delivered",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T10:00:30-03:00",
  "data": {
    "id": "msg_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
    "from": { "name": "João Silva", "number": "5511777777777" },
    "to": { "name": "Turbo Notify", "number": "5511999990001", "alias": "main" },
    "preview": "Olá, gostaria de saber...",
    "content_type": "text",
    "direction": "inbound",
    "channel": "whatsapp",
    "origin": "native",
    "at": "2026-06-01T10:00:30-03:00"
  }
}
```

```json title="message.read (1:1)"
{
  "id": "evt_read_001",
  "type": "message.read",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T10:01:15-03:00",
  "data": {
    "id": "msg_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
    "from": { "name": "João Silva", "number": "5511777777777" },
    "to": { "name": "Turbo Notify", "number": "5511999990001", "alias": "main" },
    "preview": "Olá, gostaria de saber...",
    "content_type": "text",
    "direction": "inbound",
    "channel": "whatsapp",
    "origin": "native",
    "at": "2026-06-01T10:01:15-03:00"
  }
}
```

`preview` fica vazio quando o corpo da mensagem original já foi purgado pelo retention (LGPD); `content_type` é preservado.

#### Recibos em grupo — um por participante

Quando você envia uma mensagem a um **grupo**, **cada outro participante**
gera o seu próprio `delivered` (ao receber) e `read` (ao ler). Em cada
recibo: `from` = aquele participante, `to` = o grupo. A linha tenant (linked
device) que observou o recibo é dada pelo `number_alias` do envelope.

```json title="message.read (grupo) — recibo do participante A"
{
  "id": "evt_read_grp_A",
  "type": "message.read",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T10:01:15-03:00",
  "data": {
    "id": "msg_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
    "from": { "name": "Número A", "number": "5511973117056" },
    "to": { "group_id": "grp_e442553a56b14e2695f6acfd7d651b1a", "name": "TNT" },
    "preview": "Olá, time!",
    "content_type": "text",
    "direction": "inbound",
    "channel": "whatsapp",
    "origin": "native",
    "at": "2026-06-01T10:01:15-03:00"
  }
}
```

- `from.number` muda a cada participante que confirma; `to` (o grupo) e o `number_alias` do envelope (o seu número) permanecem os mesmos para todos os recibos da mesma mensagem.
- Para detectar recibos de grupo: verifique `"group_id" in data.to`.

### Exemplos por `content_type` (inbound)

O envelope do webhook é idêntico em estrutura para todos os tipos
inbound — apenas `data.content_type` e `data.preview` mudam. Os
trechos abaixo mostram só o objeto `data` para reduzir ruído visual.

Para obter o **body completo** (texto, vCard, coordenadas, descrição
do evento, link da mídia, etc.), faça `GET /v1/messages/{id}`.

#### `text` — texto simples

```json title="data — message.received (text)"
{
  "id": "msg_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "from": { "name": "João Silva", "number": "5511777777777" },
  "to": { "number": "5511999990001", "alias": "main" },
  "preview": "Olá, gostaria de saber...",
  "content_type": "text",
  "direction": "inbound",
  "channel": "whatsapp",
  "origin": "native",
  "at": "2026-06-01T15:32:11-03:00"
}
```

> **Importante (2026-04-28):** respostas (mensagens que citam outra)
> são entregues como evento dedicado **`message.replied`** — nunca
> mais como `message.received` com `content_type=reply`.

#### `image`

```json title="data — message.received (image)"
{
  "id": "msg_a1b2c3d4e5f60718293a4b5c6d7e0010",
  "from": { "name": "João Silva", "number": "5511777777777" },
  "to": { "number": "5511999990001", "alias": "main" },
  "preview": "Foto do produto",
  "content_type": "image",
  "direction": "inbound",
  "channel": "whatsapp",
  "origin": "native",
  "at": "2026-06-01T15:34:01-03:00"
}
```

`media` foi removido dos eventos — faça `GET /v1/messages/{id}` para obter o bloco `image` com a URL de download.

#### `video`

```json title="data — message.received (video)"
{
  "id": "msg_a1b2c3d4e5f60718293a4b5c6d7e0011",
  "from": { "name": "João Silva", "number": "5511777777777" },
  "to": { "number": "5511999990001", "alias": "main" },
  "preview": "[Video]",
  "content_type": "video",
  "direction": "inbound",
  "channel": "whatsapp",
  "origin": "native",
  "at": "2026-06-01T15:35:12-03:00"
}
```

#### `audio` — voice note ou música

```json title="data — message.received (audio)"
{
  "id": "msg_a1b2c3d4e5f60718293a4b5c6d7e0012",
  "from": { "name": "João Silva", "number": "5511777777777" },
  "to": { "number": "5511999990001", "alias": "main" },
  "preview": "[Audio]",
  "content_type": "audio",
  "direction": "inbound",
  "channel": "whatsapp",
  "origin": "native",
  "at": "2026-06-01T15:36:08-03:00"
}
```

`mime_type` típico: `audio/ogg` (voice note) ou `audio/mpeg` (música). Se transcrição Whisper estiver configurada, `preview` exibe o texto transcrito.

#### `document`

```json title="data — message.received (document)"
{
  "id": "msg_a1b2c3d4e5f60718293a4b5c6d7e0013",
  "from": { "name": "João Silva", "number": "5511777777777" },
  "to": { "number": "5511999990001", "alias": "main" },
  "preview": "fatura-abril.pdf",
  "content_type": "document",
  "direction": "inbound",
  "channel": "whatsapp",
  "origin": "native",
  "at": "2026-06-01T15:37:00-03:00"
}
```

#### `sticker`

```json title="data — message.received (sticker)"
{
  "id": "msg_a1b2c3d4e5f60718293a4b5c6d7e0014",
  "from": { "name": "João Silva", "number": "5511777777777" },
  "to": { "number": "5511999990001", "alias": "main" },
  "preview": "[Sticker]",
  "content_type": "sticker",
  "direction": "inbound",
  "channel": "whatsapp",
  "origin": "native",
  "at": "2026-06-01T15:37:33-03:00"
}
```

#### `location`

```json title="data — message.received (location)"
{
  "id": "msg_a1b2c3d4e5f60718293a4b5c6d7e0001",
  "from": { "name": "João Silva", "number": "5511777777777" },
  "to": { "number": "5511999990001", "alias": "main" },
  "preview": "Av. Paulista, Sao Paulo, SP",
  "content_type": "location",
  "direction": "inbound",
  "channel": "whatsapp",
  "origin": "native",
  "at": "2026-06-01T15:38:21-03:00"
}
```

`preview` é o texto humanizado sem colchetes. Body completo via `GET /v1/messages/{id}` no campo `location` com `latitude`, `longitude`, `name`, `address`, etc.

#### `calendar` — evento/calendário (WhatsApp `eventMessage`)

```json title="data — message.received (calendar)"
{
  "id": "msg_a1b2c3d4e5f60718293a4b5c6d7e0003",
  "from": { "name": "João Silva", "number": "5511777777777" },
  "to": { "number": "5511999990001", "alias": "main" },
  "preview": "Sprint Planning — qui, 1 jun 2026 14:00",
  "content_type": "calendar",
  "direction": "inbound",
  "channel": "whatsapp",
  "origin": "native",
  "at": "2026-06-01T15:40:00-03:00"
}
```

Body completo via `GET /v1/messages/{id}` no campo `calendar` com `name`, `description`, `start_at`, `end_at`, etc.

:::tip
Quer ver o body renderizado de forma visual antes de plugar em
produção? Abra o <ExternalLink href="https://webhook.turbonotify.com">Webhook Inspector</ExternalLink>
e use **Send sample**.
:::

## Tipos de evento

O catálogo público é fechado — seu endpoint só recebe os 24 eventos listados aqui.

### Mensagens — ciclo de envio

| Tipo                      | Quando é emitido                                                   |
| ------------------------- | ------------------------------------------------------------------ |
| `message.sent`            | Mensagem enviada com sucesso ao WhatsApp (sem citação).            |
| `message.send.failed`     | Falha no envio de uma mensagem regular (sem citação).              |
| `message.delivered`       | Receipt inbound: peer entregou uma mensagem que você enviou.       |
| `message.read`            | Receipt inbound: peer leu uma mensagem que você enviou.            |

### Mensagens — replies (citações)

| Tipo                      | Quando é emitido                                                   |
| ------------------------- | ------------------------------------------------------------------ |
| `message.replied`         | Reply enviado/recebido (qualquer `content_type`, ambas direções).  |
| `message.reply.failed`    | Falha ao enviar um reply (via `POST /v1/messages/{message_id}/reply`). |

### Mensagens — recebimento e operações

| Tipo                      | Quando é emitido                                                   |
| ------------------------- | ------------------------------------------------------------------ |
| `message.received`        | Mensagem recebida de um contato (texto, mídia — sem citação).      |
| `message.edited`          | Mensagem foi editada (sua ou do contato).                          |
| `message.edit.failed`     | Falha ao editar uma mensagem.                                      |
| `message.deleted`         | Mensagem foi deletada/revogada (sua ou do contato).                |
| `message.delete.failed`   | Falha ao deletar uma mensagem.                                     |
| `message.reacted`         | Uma reação foi adicionada ou removida (`preview` = emoji ou `""`). |
| `message.react.failed`    | Falha ao reagir a uma mensagem.                                    |

### Números — ciclo de vida

| Tipo                          | Quando é emitido                                                  |
| ----------------------------- | ----------------------------------------------------------------- |
| `number.created`              | Número extra provisionado.                                        |
| `number.connected`            | Número principal ou extra conectado ao WhatsApp.                  |
| `number.disconnected`         | Número desconectado temporariamente.                              |
| `number.connect.failed`       | Falha no pareamento/conexão.                                      |
| `number.revoked`              | Dispositivo desvinculado pelo WhatsApp.                           |
| `number.remove.scheduled`     | Exclusão de número extra agendada (período de carência).          |
| `number.remove.canceled`      | Exclusão agendada foi cancelada.                                  |
| `number.removed`              | Número extra foi removido.                                        |

Todos os eventos `number.*` carregam os campos comuns `alias` e `number` (quando disponível). O campo `name` — nome de exibição WhatsApp do número, capturado na conexão — é **opcional** em todos os eventos e **omitido quando nulo**. O `alias` `"main"` identifica o número principal; qualquer outro valor identifica um número extra.

Todos os eventos `number.*` também carregam o par de coerência `channel` (sempre `"whatsapp"`) e `origin` (sempre `"tn"` — o ciclo de vida do dispositivo é produzido pela plataforma Turbo Notify). Eventos `number.*` **não** carregam `direction` — o ciclo de vida do link (conexão, desconexão, revogação, remoção, criação) não é um fluxo de mensagem WhatsApp.

O campo `at` (em `data`) segue a classificação de 2026-06-02 (momento da ação real no WhatsApp): os eventos de ação real **`number.connected`**, **`number.disconnected`**, **`number.revoked`** e **`number.connect.failed`** carregam `at`; os eventos sintéticos do TN — **`number.created`**, **`number.remove.scheduled`**, **`number.remove.canceled`** e **`number.removed`** — **não** carregam `at`, apenas o `created_at` do envelope (`number.remove.scheduled` mantém `scheduled_at` como timestamp de valor de negócio, com seu nome semântico).

```json title="number.connected (exemplo)"
{
  "id": "evt_num_conn_abc123",
  "type": "number.connected",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T10:00:00-03:00",
  "data": {
    "name": "Turbo Notify",
    "alias": "main",
    "number": "5511973117056",
    "channel": "whatsapp",
    "origin": "tn",
    "at": "2026-06-01T10:00:00-03:00"
  }
}
```

### Armazenamento e mídia

Eventos relacionados à integração BYO Object Storage (AWS S3, Cloudflare R2, Supabase Storage) e à disponibilidade de mídia recebida.

:::note
Estes eventos são **provider-agnostic**: o payload é o mesmo independentemente do vendor configurado.
:::

:::caution[Mensagens recebidas com mídia — entrega só após resolução]
Para mensagens **inbound com mídia** (imagem, vídeo, áudio, documento, sticker), o webhook `message.received` só é enviado depois que o estado de mídia chega a um *terminal* para o seu tenant:

- **Com [BYO Storage](/general/byo-storage-setup/) ativo**: bytes salvos no seu bucket, ou falha permanente sinalizada por `media.unavailable`.
- **Sem BYO Storage**: payload sai com `media` indisponível e `reason = "no_storage"`. O Turbo Notify **não baixa nem proxia** os bytes da CDN do WhatsApp.

Use `GET /v1/messages/{id}` para acessar o bloco `image`/`video`/`audio`/`document`/`sticker` com a URL de download (quando disponível).
:::

#### Saúde do storage

| Tipo                          | Quando é emitido                                                  |
| ----------------------------- | ----------------------------------------------------------------- |
| `storage.degraded`            | Falhas consecutivas na integração BYO Object Storage abriram o circuito. |
| `storage.recovered`           | Primeira operação bem-sucedida após o circuito ter sido aberto.  |

#### Disponibilidade de mídia

| Tipo                          | Quando é emitido                                                  |
| ----------------------------- | ----------------------------------------------------------------- |
| `media.unavailable`           | Mídia não pôde ser persistida no bucket BYO (`reason`: `no_storage`, `storage_circuit_open`, `upload_failed`). |

#### Exemplo — `storage.degraded`

```json title="webhook-payload.json"
{
  "id": "evt_storage_deg_abc123def4567890",
  "type": "storage.degraded",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "created_at": "2026-06-01T10:00:00-03:00",
  "data": {
    "reason": "upload_failed",
    "consecutive_failures": 5,
    "error_code": "access_denied",
    "affected_messages_count": 3,
    "channel": "whatsapp",
    "origin": "tn"
  }
}
```

#### Exemplo — `storage.recovered`

```json title="webhook-payload.json"
{
  "id": "evt_storage_rec_def789abc2345678",
  "type": "storage.recovered",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "created_at": "2026-06-01T10:08:00-03:00",
  "data": {
    "degraded_since": "2026-06-01T10:00:00-03:00",
    "duration_seconds": 480,
    "channel": "whatsapp",
    "origin": "tn"
  }
}
```

#### Exemplo — `media.unavailable`

```json title="webhook-payload.json"
{
  "id": "evt_media_unav_ghi456jkl7890123",
  "type": "media.unavailable",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "created_at": "2026-06-01T11:15:30-03:00",
  "data": {
    "message_id": "msg_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
    "content_type": "image",
    "direction": "inbound",
    "preview": "Foto",
    "reason": "no_storage",
    "channel": "whatsapp",
    "origin": "tn"
  }
}
```

## Eventos de falha

Os eventos de falha compartilham um envelope comum e diferem entre si pelo conjunto de campos extras. `id` pode ser `null` **apenas** em `message.send.failed` (quando a row não foi criada).

### `message.send.failed` / `message.reply.failed`

```json title="message.send.failed"
{
  "id": "evt_send_fail_001",
  "type": "message.send.failed",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T10:00:00-03:00",
  "data": {
    "id": null,
    "preview": "Mensagem que falhou",
    "content_type": "text",
    "direction": "outbound",
    "channel": "whatsapp",
    "origin": "tn",
    "at": "2026-06-01T10:00:00-03:00",
    "error": {
      "code": "whatsapp_unavailable",
      "message": "WhatsApp session is not connected",
      "hint": null
    }
  }
}
```

### `message.edit.failed` / `message.delete.failed` / `message.react.failed`

Diferentemente de `message.send.failed`, essas operações falham **sobre uma mensagem que já existe** — `id` está sempre presente.

```json title="message.edit.failed"
{
  "id": "evt_edit_fail_001",
  "type": "message.edit.failed",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T10:05:00-03:00",
  "data": {
    "id": "msg_01993c5a1b24d3e9f01a2b3c4d5e6f70",
    "preview": "Texto que tentou editar",
    "content_type": "text",
    "direction": "outbound",
    "channel": "whatsapp",
    "origin": "tn",
    "at": "2026-06-01T10:05:00-03:00",
    "error": {
      "code": "edit_window_expired",
      "message": "Cannot edit after 15 minutes",
      "hint": null
    }
  }
}
```

`message.delete.failed` e `message.react.failed` seguem a mesma forma — apenas o valor de `type` muda.

## Mensagens de grupo nos webhooks outbound

Quando você envia uma mensagem para um grupo via
`POST /v1/messages` com `to.group_id`, os webhooks subsequentes
(`message.sent`, `message.delivered`, `message.read`,
`message.send.failed`) usam o mesmo shape polimórfico — `to` carrega
`group_id` + snapshot `name`, e o `number_alias` do envelope identifica a linha que enviou:

```json title="message.sent (outbound para grupo)"
{
  "id": "evt_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "type": "message.sent",
  "organization_id": "org_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4",
  "number_alias": "main",
  "created_at": "2026-06-01T10:00:05-03:00",
  "data": {
    "id": "msg_01993c5a1b24d3e9f01a2b3c4d5e6f70",
    "from": { "name": "Turbo Notify", "number": "5511973117056", "alias": "main" },
    "to": { "group_id": "grp_e442553a56b14e2695f6acfd7d651b1a", "name": "TNT" },
    "preview": "Bom dia, equipe",
    "content_type": "text",
    "direction": "outbound",
    "channel": "whatsapp",
    "origin": "tn",
    "at": "2026-06-01T10:00:05-03:00"
  }
}
```

- `to.group_id` é o identificador público `grp_{hex32}` — mesmo valor fornecido no `POST /v1/messages`.
- O `number_alias` do envelope identifica a linha Turbo Notify que enviou a mensagem ao grupo.
- Para detectar mensagens de grupo: verifique `"group_id" in data.to`.

## Mapeamento de operações assíncronas

Operações da API que disparam processamento assíncrono expõem o `X-Request-ID` no header da resposta. Use `GET /v1/events?request_id=req_...` para consultar os eventos da operação e identificar o resultado pelo `id` da mensagem (`data.id`):

| Operação                   | Evento de sucesso             | Evento de falha                   |
| -------------------------- | ----------------------------- | --------------------------------- |
| Enviar mensagem            | `message.sent`                | `message.send.failed`             |
| Responder mensagem         | `message.replied`             | `message.reply.failed`            |
| Editar mensagem            | `message.edited`              | `message.edit.failed`             |
| Deletar mensagem           | `message.deleted`             | `message.delete.failed`           |
| Reagir a uma mensagem      | `message.reacted`             | `message.react.failed`            |
| Conectar número extra      | `number.connected`            | `number.connect.failed`           |
| Agendar remoção de número  | `number.remove.scheduled`     | —                                 |
| Cancelar remoção agendada  | `number.remove.canceled`      | —                                 |

## Tentativas de entrega

Se seu endpoint não responder com status `2xx` dentro de 5 segundos, o Turbo Notify refará a tentativa com backoff exponencial:

| Tentativa | Intervalo aproximado |
| :-------: | -------------------- |
| 1         | Imediata             |
| 2         | 12 segundos          |
| 3         | 24 segundos          |
| 4         | 48 segundos          |
| 5         | 96 segundos          |
| 6         | 192 segundos         |

Após esgotar as tentativas, o evento é marcado como `dead_letter` e descartado.

## Garantia de entrega e idempotência

O Turbo Notify adota o modelo **at-least-once**. Em situações excepcionais, o mesmo evento pode ser entregue mais de uma vez. Use o campo `id` para implementar idempotência e evitar processamento duplicado.

### O campo `id` como chave de idempotência

O campo `id` (ex.: `evt_abc123…`) identifica o evento de forma única e é **estável entre tentativas de entrega**. Se o mesmo `id` chegar duas vezes no seu endpoint, trata-se de uma reentrega automática pelo Turbo Notify — o evento já foi processado na primeira entrega.

Armazene os `id` recebidos e ignore eventos cujo `id` já esteja na sua base:

```js title="dedup.js"
const seen = new Set();

function handleEvent(event) {
  if (seen.has(event.id)) {
    return; // reentrega — já processado
  }
  seen.add(event.id);
  // ... processar
}
```

Em produção, persista o `id` em uma tabela ou cache com TTL de ~72 h.

### Timestamps em reentregas

O campo `created_at` (envelope) reflete o **momento de emissão do evento pelo Turbo Notify** — quando o registro do evento é criado e publicado, nunca a hora em que o webhook foi enviado ao seu endpoint. Quando presente, `at` (em `data`) reflete o **momento da ação real no WhatsApp**. Desde 2026-06-02 estes são **valores distintos**: um evento pode ser emitido algum tempo depois da ação que descreve (eventos sintéticos do TN não têm `at`). Em reentregas, tanto `created_at` quanto `at` permanecem **inalterados** — são os mesmos valores da tentativa original.

Isso significa que, ao receber uma reentrega:
- `created_at` é o momento de emissão; `at` (quando presente) é o momento da ação no WhatsApp — ambos fixos entre tentativas.
- `X-Webhook-Attempt` é incrementado a cada tentativa — use-o para diagnóstico, não para lógica de negócio.
- `X-Webhook-Timestamp` reflete a hora do **envio HTTP atual** e é usado para validação de assinatura e proteção anti-replay; **não** é o timestamp do evento.

## Alternativa: polling

Se preferir consumir eventos sob demanda em vez de expor um endpoint público, utilize o [polling via `GET /events`](/messages/polling/). O objeto `data` é o mesmo do webhook.
