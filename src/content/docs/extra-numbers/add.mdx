---
title: Inclusão de Números Extra
description: Como adicionar números extras de WhatsApp à sua conta no Turbo Notify para envio de mensagens com múltiplos remetentes.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ApiResponse from '@components/ApiResponse.astro';
import DashboardLink from '@components/DashboardLink.astro';
import ExternalLink from '@components/ExternalLink.astro';

Números extras permitem que você envie mensagens a partir de múltiplos números de WhatsApp, além do número principal da sua conta. Cada número extra é vinculado via código de pareamento.

## Cadastro pelo Painel

A forma mais simples de adicionar um número extra é pelo painel do Turbo Notify. Acesse a página de <DashboardLink path="/extra-numbers">Números Extra</DashboardLink> e clique em **Adicionar número**.

Você deverá informar o número de telefone com código do país e aguardar o código de pareamento que será exibido na tela. Insira esse código no WhatsApp do aparelho correspondente para concluir a vinculação.

## Cadastro via API

Também é possível cadastrar números extras de forma programática pela API.

<Tabs>
  <TabItem label="cURL">
    ```bash title="turbo-notify.sh"
    curl -X POST \
      https://api.turbonotify.com/extra-numbers \
      -H "Authorization: Bearer SUA_CHAVE" \
      -H "Content-Type: application/json" \
      -d '{
        "phone": "+5511988888888"
      }'
    ```
  </TabItem>
  <TabItem label="Node">
    ```js title="turbo-notify.js"
    const response = await fetch('https://api.turbonotify.com/extra-numbers', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer SUA_CHAVE',
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        phone: '+5511988888888'
      })
    });

    const result = await response.json();
    console.log(result);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python title="turbo_notify.py"
    import requests

    url = "https://api.turbonotify.com/extra-numbers"
    headers = {
        "Authorization": "Bearer SUA_CHAVE",
        "Content-Type": "application/json"
    }
    data = {
        "phone": "+5511988888888"
    }

    response = requests.post(url, json=data, headers=headers)
    result = response.json()
    print(result)
    ```
  </TabItem>
  <TabItem label="Go">
    ```go title="turbo_notify.go"
    package main

    import (
        "fmt"
        "io"
        "net/http"
        "strings"
    )

    func main() {
        payload := strings.NewReader(`{"phone": "+5511988888888"}`)
        req, _ := http.NewRequest("POST", "https://api.turbonotify.com/extra-numbers", payload)
        req.Header.Set("Authorization", "Bearer SUA_CHAVE")
        req.Header.Set("Content-Type", "application/json")
        resp, _ := http.DefaultClient.Do(req)
        defer resp.Body.Close()
        body, _ := io.ReadAll(resp.Body)
        fmt.Println(string(body))
    }
    ```
  </TabItem>
  <TabItem label="PHP">
    ```php title="turbo-notify.php"
    <?php
    $url = 'https://api.turbonotify.com/extra-numbers';
    $data = [
        'phone' => '+5511988888888'
    ];

    $options = [
        'http' => [
            'header' => [
                'Authorization: Bearer SUA_CHAVE',
                'Content-Type: application/json'
            ],
            'method' => 'POST',
            'content' => json_encode($data)
        ]
    ];

    $context = stream_context_create($options);
    $result = file_get_contents($url, false, $context);
    $response = json_decode($result, true);
    var_dump($response);
    ?>
    ```
  </TabItem>
  <TabItem label="C#">
    ```csharp title="TurboNotify.cs"
    using System.Text;
    using System.Text.Json;

    var client = new HttpClient();
    client.DefaultRequestHeaders.Add("Authorization", "Bearer SUA_CHAVE");

    var data = new {
        phone = "+5511988888888"
    };

    var json = JsonSerializer.Serialize(data);
    var content = new StringContent(json, Encoding.UTF8, "application/json");

    var response = await client.PostAsync(
        "https://api.turbonotify.com/extra-numbers",
        content
    );
    var result = await response.Content.ReadAsStringAsync();
    Console.WriteLine(result);
    ```
  </TabItem>
  <TabItem label="Java">
    ```java title="TurboNotify.java"
    import java.net.http.*;
    import java.net.URI;

    public class TurboNotify {
        public static void main(String[] args) throws Exception {
            var client = HttpClient.newHttpClient();

            var json = """
                {
                    "phone": "+5511988888888"
                }
                """;

            var request = HttpRequest.newBuilder()
                .uri(URI.create("https://api.turbonotify.com/extra-numbers"))
                .header("Authorization", "Bearer SUA_CHAVE")
                .header("Content-Type", "application/json")
                .POST(HttpRequest.BodyPublishers.ofString(json))
                .build();

            var response = client.send(request,
                HttpResponse.BodyHandlers.ofString());
            System.out.println(response.body());
        }
    }
    ```
  </TabItem>
</Tabs>

A resposta conterá o código de pareamento que deve ser inserido no WhatsApp do aparelho.

<ApiResponse
  status={200}
  body={{
    code: 'AAAA-BBBB',
  }}
/>

## Usabilidade

- Cada número extra deve pertencer a um aparelho com WhatsApp ativo.
- O código de pareamento tem validade limitada. Caso expire, será necessário refazer a solicitação.
- Você pode definir um alias para o número extra durante o cadastro, facilitando o uso no envio de mensagens via campo `from`.
- Após o pareamento, o número ficará com status `pending` até que a vinculação seja confirmada.

## Status

Após cadastrar o número extra, acompanhe o andamento da vinculação consultando o [Status dos Números Extra](/extra-numbers/status/).

## Erros possíveis

Veja os principais erros que podem ocorrer no cadastro de números extras:

| Status | Código de erro           | Descrição                                              |
| :----: | ------------------------ | ------------------------------------------------------ |
| `400`  | `invalid_json`           | O JSON da requisição está malformado ou inválido.      |
| `402`  | `not_available_in_plan`  | O plano atual não suporta números extras.              |
| `403`  | `not_authorized`         | A chave de acesso está inválida ou ausente.            |
| `409`  | `number_already_exists`  | O número informado já está cadastrado na conta.        |
| `500`  | `internal_error`         | Erro interno inesperado no servidor.                   |
| `503`  | `whatsapp_unavailable`   | O WhatsApp está temporariamente fora do ar.            |
