---
title: Refresh Contact
description: Refresh a contact via POST /v1/numbers/{alias}/contacts/{id}/refresh. Translation pending; see the canonical PT-BR version.
---

> This page is being translated. See the PT-BR (canonical) version: [/contacts/refresh/](/contacts/refresh/).

```http title="Refresh a contact"
POST /v1/numbers/{alias}/contacts/{id}/refresh
```

Forces a re-fetch of the contact's WhatsApp metadata (name, profile picture).
`{alias}` is the contact's `number_alias` — take it from the event envelope.

```bash
curl -X POST \
  https://api.turbonotify.com/v1/contacts/ct_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4/refresh \
  -H "Authorization: Bearer tn_live_..."
```

For extra numbers: `POST /v1/numbers/{alias}/contacts/{id}/refresh`
