---
title: Status dos números extras
description: Como consultar o status de vinculação e funcionamento dos números extras cadastrados no Turbo Notify.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ApiResponse from '@components/ApiResponse.astro';

Acompanhe o status de cada número extra cadastrado na sua conta para garantir que esteja ativo e pronto para envio.

## Cobrança

A cobrança de números extras é feita de forma proporcional ao período de uso. Para entender os valores e o ciclo de cobrança, consulte a documentação de [Cobrança de números extras](/numbers/billing/).

## Consultar status

Para verificar o status de um número extra específico, faça uma requisição GET informando o alias do número.

<Tabs>
  <TabItem label="cURL">
    ```bash title="turbo-notify.sh"
    curl -X GET \
      https://api.turbonotify.com/v1/numbers/ALIAS_DO_NUMERO \
      -H "Authorization: Bearer SUA_CHAVE"
    ```
  </TabItem>
  <TabItem label="Node">
    ```js title="turbo-notify.js"
    const response = await fetch('https://api.turbonotify.com/v1/numbers/ALIAS_DO_NUMERO', {
      method: 'GET',
      headers: {
        'Authorization': 'Bearer SUA_CHAVE'
      }
    });

    const result = await response.json();
    console.log(result);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python title="turbo_notify.py"
    import requests

    url = "https://api.turbonotify.com/v1/numbers/ALIAS_DO_NUMERO"
    headers = {
        "Authorization": "Bearer SUA_CHAVE"
    }

    response = requests.get(url, headers=headers)
    result = response.json()
    print(result)
    ```
  </TabItem>
  <TabItem label="Go">
    ```go title="turbo_notify.go"
    package main

    import (
        "fmt"
        "io"
        "net/http"
    )

    func main() {
        req, _ := http.NewRequest("GET", "https://api.turbonotify.com/v1/numbers/ALIAS_DO_NUMERO", nil)
        req.Header.Set("Authorization", "Bearer SUA_CHAVE")
        resp, _ := http.DefaultClient.Do(req)
        defer resp.Body.Close()
        body, _ := io.ReadAll(resp.Body)
        fmt.Println(string(body))
    }
    ```
  </TabItem>
  <TabItem label="PHP">
    ```php title="turbo-notify.php"
    <?php
    $url = 'https://api.turbonotify.com/v1/numbers/ALIAS_DO_NUMERO';

    $options = [
        'http' => [
            'header' => [
                'Authorization: Bearer SUA_CHAVE'
            ],
            'method' => 'GET'
        ]
    ];

    $context = stream_context_create($options);
    $result = file_get_contents($url, false, $context);
    $response = json_decode($result, true);
    var_dump($response);
    ?>
    ```
  </TabItem>
  <TabItem label="C#">
    ```csharp title="TurboNotify.cs"
    var client = new HttpClient();
    client.DefaultRequestHeaders.Add("Authorization", "Bearer SUA_CHAVE");

    var response = await client.GetAsync(
        "https://api.turbonotify.com/v1/numbers/ALIAS_DO_NUMERO"
    );
    var result = await response.Content.ReadAsStringAsync();
    Console.WriteLine(result);
    ```
  </TabItem>
  <TabItem label="Java">
    ```java title="TurboNotify.java"
    import java.net.http.*;
    import java.net.URI;

    public class TurboNotify {
        public static void main(String[] args) throws Exception {
            var client = HttpClient.newHttpClient();

            var request = HttpRequest.newBuilder()
                .uri(URI.create("https://api.turbonotify.com/v1/numbers/ALIAS_DO_NUMERO"))
                .header("Authorization", "Bearer SUA_CHAVE")
                .GET()
                .build();

            var response = client.send(request,
                HttpResponse.BodyHandlers.ofString());
            System.out.println(response.body());
        }
    }
    ```
  </TabItem>
</Tabs>

**Escopo obrigatório:** `numbers:read`

<ApiResponse
  status={200}
  body={{
    contact_id: 'ct_c7abb1f5ee9640bf86a0a20290810ae9',
    name: 'Condomínio Alpha',
    type: 'extra',
    alias: 'condominio_1',
    number: '5511999999997',
    status: 'connected',
    at: '2025-07-16T10:01:00Z',
    description: null,
    scheduled_deletion_at: null,
    reason: null,
    pairing: null,
  }}
/>

:::note[Status do número principal]
O número principal tem sua própria rota de status, `GET /v1/numbers/main`
(mesmo escopo, `numbers:read`), com uma resposta mais enxuta: sem os
campos `description`, `scheduled_deletion_at`, `reason` e `pairing` (o
pareamento do número principal é feito exclusivamente pelo dashboard, não
tem contrapartida na API pública):

```json
{
  "contact_id": "ct_c7abb1f5ee9640bf86a0a20290810ae9",
  "name": "Laboratório Central",
  "type": "main",
  "alias": "main",
  "number": "5511973117056",
  "status": "connected",
  "at": "2026-04-07T10:00:00Z"
}
```
:::

:::note[`description`, `scheduled_deletion_at` e `reason` sempre `null` aqui]
Diferente de [Listagem](/numbers/list/), que retorna a `description` real
cadastrada, este endpoint (`GET /v1/numbers/{alias}`) hoje retorna
`description`, `scheduled_deletion_at` **e `reason`** sempre como `null`,
independente do valor real: nenhum motivo de falha é populado hoje, mesmo
quando `status: "failed"`. Quando o número está com `status:
"pending_deletion"`, o timestamp da exclusão agendada aparece no campo `at`
(não em `scheduled_deletion_at`). Para a descrição cadastrada, consulte
[Listagem](/numbers/list/).
:::

:::note[`at` acompanha o status atual]
`at` é o momento em que o número **entrou no status atual**: atualiza a
cada transição (pareamento, conexão, queda de conexão, reconexão, falha,
revogação). Um número `disconnected`, `failed`, `reconnecting` etc. reporta
o instante dessa própria transição, então `desconectado desde {at}` /
`falhou em {at}` exibem o horário certo diretamente.

Um número recém-criado, sem link de dispositivo ainda (`status: "pending"`),
não tem transição alguma registrada: `at` vem como **string vazia** (`""`).
Trate isso antes de converter para data: `Date.parse("")` (JS) devolve
`NaN`, e `datetime.fromisoformat("")` (Python) levanta `ValueError`.

A única exceção é `status: "pending_deletion"`, em que `at` traz o momento
da exclusão agendada (veja a nota acima).
:::

## Campos da resposta

| Campo | Tipo | Descrição |
| ----- | ---- | --------- |
| `contact_id` | string\|null | Identificador público (`ct_…`) do contato próprio do número; `null` enquanto não resolvido |
| `name` | string\|null | Nome da conta WhatsApp do número (capturado na conexão); `null` enquanto não houver conexão estabelecida |
| `type` | string | Sempre `"extra"` neste endpoint (números extras), para o principal use `GET /v1/numbers/main` |
| `alias` | string | Identificador único do número extra na conta |
| `number` | string | Número de telefone em dígitos E.164 (sem `+`) |
| `status` | string | Estado atual, veja [Tabela de status](#tabela-de-status) |
| `at` | string | Timestamp ISO 8601 do momento em que o número **entrou no status atual** (`status`): atualiza a cada transição. **String vazia (`""`)** apenas quando o número ainda não tem link de dispositivo (`status: "pending"`, nenhuma transição registrada). Exceção: quando `status = "pending_deletion"`, é o momento da exclusão agendada. |
| `description` | string\|null | Sempre `null` neste endpoint hoje, veja nota acima |
| `scheduled_deletion_at` | string\|null | Sempre `null` neste endpoint hoje, veja nota acima |
| `reason` | string\|null | Reservado para o motivo de falha: **hoje sempre `null`**, veja a nota acima |
| `pairing` | object\|null | Informações de pareamento: presente apenas durante o processo de vinculação |

### Bloco de pareamento (`pairing`)

Presente quando `status` é `pairing` e o código ainda está válido:

| Campo | Tipo | Descrição |
| ----- | ---- | --------- |
| `method` | string | Método de pareamento: `"pairing_code"` |
| `data` | string | Código alfanumérico de 8 caracteres a ser inserido no WhatsApp (exibido com hífen, ex.: `Z5X7-LTYL`) |
| `expires_at` | string | Timestamp ISO 8601 de expiração do código |

## Tabela de status

| Status              | Descrição                                                                 |
| -------------------- | -------------------------------------------------------------------------- |
| `pending`            | Número cadastrado; aguardando início do pareamento.                       |
| `pairing`            | Código de pareamento gerado; aguardando inserção no WhatsApp.             |
| `inactive`           | Autenticação obtida, mas a conexão ainda não foi estabelecida.            |
| `connecting`         | Conexão sendo estabelecida (`POST .../connect` aceito).                   |
| `connected`          | O número está vinculado e pronto para envio de mensagens.                 |
| `disconnected`       | Conexão perdida ou desativada; a autenticação é preservada para reconectar. |
| `reconnecting`       | Reconexão automática em andamento após uma queda.                         |
| `failed`             | A vinculação falhou. Verifique o motivo abaixo.                           |
| `revoked`            | O dispositivo foi desvinculado pelo WhatsApp.                             |
| `pending_deletion`   | Exclusão agendada (`DELETE /v1/numbers/{alias}`): sobrepõe qualquer outro status até a exclusão. |

## Motivos de falha

O campo `reason` foi projetado para indicar o motivo quando `status` é
`failed`, mas **ainda não está implementado**: a rota sempre retorna
`reason: null`, mesmo em `status: "failed"` (veja a nota acima). Os valores
abaixo descrevem a intenção original e **ainda não são emitidos** pela API:

| Motivo                  | Descrição                                                     |
| ----------------------- | ------------------------------------------------------------- |
| `no_active_session`     | A organização não tem uma sessão ativa para parear o número extra através dela. |
| `pair_phone_timeout`    | O WhatsApp não retornou um código de pareamento em 30s.       |
| `pair_phone_error`      | O WhatsApp retornou um erro ao gerar o código.                |
| `pair_qr_timeout`       | O QR code não foi gerado em 20s.                               |
| `pair_qr_error`         | O WhatsApp retornou um erro ao gerar o QR code.                |
| `pairing_timeout`       | O código foi gerado, mas não foi inserido dentro da janela de validade. |
| `pairing_monitor_error` | Erro interno inesperado ao monitorar a finalização do pareamento. |

## Erros possíveis

| Status | Código de erro          | Descrição                                           |
| :----: | ----------------------- | --------------------------------------------------- |
| `402`  | `feature_not_available` | O plano atual não suporta números extras.           |
| `403`  | `not_authorized`        | A chave de acesso está inválida ou ausente.         |
| `403`  | `insufficient_scope`    | A chave de acesso não possui o escopo `numbers:read` (resposta inclui `required_scope`). |
| `404`  | `extra_number_not_found` | O alias informado não foi encontrado na conta.     |
| `500`  | `internal_error`        | Erro interno inesperado no servidor.                |
