---
title: Listagem de números extras
description: Como listar todos os números extras cadastrados na sua conta do Turbo Notify.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import ApiResponse from '@components/ApiResponse.astro';

Liste todos os números extras vinculados à sua conta para consultar alias, número de telefone e status de cada um.

## Listar números extras

<Tabs>
  <TabItem label="cURL">
    ```bash title="turbo-notify.sh"
    curl -X GET \
      https://api.turbonotify.com/v1/numbers \
      -H "Authorization: Bearer SUA_CHAVE"
    ```
  </TabItem>
  <TabItem label="Node">
    ```js title="turbo-notify.js"
    const response = await fetch('https://api.turbonotify.com/v1/numbers', {
      method: 'GET',
      headers: {
        'Authorization': 'Bearer SUA_CHAVE'
      }
    });

    const result = await response.json();
    console.log(result);
    ```
  </TabItem>
  <TabItem label="Python">
    ```python title="turbo_notify.py"
    import requests

    url = "https://api.turbonotify.com/v1/numbers"
    headers = {
        "Authorization": "Bearer SUA_CHAVE"
    }

    response = requests.get(url, headers=headers)
    result = response.json()
    print(result)
    ```
  </TabItem>
  <TabItem label="Go">
    ```go title="turbo_notify.go"
    package main

    import (
        "fmt"
        "io"
        "net/http"
    )

    func main() {
        req, _ := http.NewRequest("GET", "https://api.turbonotify.com/v1/numbers", nil)
        req.Header.Set("Authorization", "Bearer SUA_CHAVE")
        resp, _ := http.DefaultClient.Do(req)
        defer resp.Body.Close()
        body, _ := io.ReadAll(resp.Body)
        fmt.Println(string(body))
    }
    ```
  </TabItem>
  <TabItem label="PHP">
    ```php title="turbo-notify.php"
    <?php
    $url = 'https://api.turbonotify.com/v1/numbers';

    $options = [
        'http' => [
            'header' => [
                'Authorization: Bearer SUA_CHAVE'
            ],
            'method' => 'GET'
        ]
    ];

    $context = stream_context_create($options);
    $result = file_get_contents($url, false, $context);
    $response = json_decode($result, true);
    var_dump($response);
    ?>
    ```
  </TabItem>
  <TabItem label="C#">
    ```csharp title="TurboNotify.cs"
    var client = new HttpClient();
    client.DefaultRequestHeaders.Add("Authorization", "Bearer SUA_CHAVE");

    var response = await client.GetAsync(
        "https://api.turbonotify.com/v1/numbers"
    );
    var result = await response.Content.ReadAsStringAsync();
    Console.WriteLine(result);
    ```
  </TabItem>
  <TabItem label="Java">
    ```java title="TurboNotify.java"
    import java.net.http.*;
    import java.net.URI;

    public class TurboNotify {
        public static void main(String[] args) throws Exception {
            var client = HttpClient.newHttpClient();

            var request = HttpRequest.newBuilder()
                .uri(URI.create("https://api.turbonotify.com/v1/numbers"))
                .header("Authorization", "Bearer SUA_CHAVE")
                .GET()
                .build();

            var response = client.send(request,
                HttpResponse.BodyHandlers.ofString());
            System.out.println(response.body());
        }
    }
    ```
  </TabItem>
</Tabs>

<ApiResponse
  status={200}
  body={{
    data: [
      {
        contact_id: 'ct_c7abb1f5ee9640bf86a0a20290810ae9',
        name: 'Condomínio Alpha',
        type: 'extra',
        alias: 'condominio_1',
        number: '5511999999997',
        description: 'Avisos do condomínio 1',
        status: { status: 'connected', at: '2025-07-16T10:01:00Z', reason: null },
      },
      {
        contact_id: null,
        name: null,
        type: 'extra',
        alias: 'condominio_2',
        number: '5511999999998',
        description: null,
        status: { status: 'connected', at: '2025-07-18T14:22:00Z', reason: null },
      },
      {
        contact_id: null,
        name: null,
        type: 'extra',
        alias: 'condominio_3',
        number: '5511999999999',
        description: null,
        status: { status: 'pending', at: '2025-07-20T09:45:00Z', reason: null },
      },
    ],
    cursor: null,
    has_more: false,
  }}
/>

**Escopo obrigatório:** `numbers:read`

## Campos da resposta

| Campo | Tipo | Descrição |
| ----- | ---- | --------- |
| `data` | array | Lista de números extras cadastrados |
| `cursor` | string\|null | Token opaco para próxima página; `null` quando não há mais itens |
| `has_more` | boolean | `true` se há mais páginas disponíveis |
| `data[].contact_id` | string\|null | Identificador público (`ct_…`) do contato próprio do número; `null` enquanto não resolvido |
| `data[].name` | string\|null | Nome da conta WhatsApp do número (capturado na conexão); `null` enquanto não houver conexão estabelecida |
| `data[].type` | string | Sempre `"extra"` para números extras |
| `data[].alias` | string | Identificador único do número extra na conta |
| `data[].number` | string | Número de telefone em dígitos E.164 (sem `+`) |
| `data[].description` | string\|null | Descrição livre definida pelo usuário |
| `data[].status` | object | `{status, at, reason?}` — estado atual do número |
| `data[].status.status` | string | `active`, `inactive`, `pending`, `pairing`, `failed` ou `revoked` |
| `data[].status.at` | string | Timestamp ISO 8601 da última mudança de status |
| `data[].status.reason` | string\|null | Motivo de falha quando `status = "failed"`; `null` caso contrário |

## Erros possíveis

| Status | Código de erro          | Descrição                                           |
| :----: | ----------------------- | --------------------------------------------------- |
| `402`  | `feature_not_available` | O plano atual não suporta números extras.           |
| `403`  | `not_authorized`        | A chave de acesso está inválida ou ausente.         |
| `403`  | `insufficient_scope`    | A chave de acesso não possui o escopo `numbers:read` (resposta inclui `required_scope`). |
| `500`  | `internal_error`        | Erro interno inesperado no servidor.                |
