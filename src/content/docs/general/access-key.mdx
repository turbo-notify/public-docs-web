---
title: Chave de acesso
description: Como criar, gerenciar e revogar chaves de acesso para autenticar suas requisições na API Turbo Notify.
---

import DashboardLink from '@components/DashboardLink.astro';

Para utilizar a API do Turbo Notify, é indispensável possuir uma chave de acesso válida.

O gerenciamento dessas chaves é feito de forma simples e segura pelo dashboard, permitindo total controle sobre quem pode interagir com sua conta e seus números.

## Inclusão

Novas chaves podem ser geradas diretamente no dashboard, acessando o menu <DashboardLink path="/api-keys">Chaves de Acesso</DashboardLink>. Não há limite de chaves imposto pela API, facilitando integrações com diferentes sistemas ou equipes.

O valor da chave é exibido apenas uma vez, no momento da criação. Por segurança, copie e armazene imediatamente, pois não será possível visualizar novamente.

## Cabeçalho

Toda requisição à API deve conter o header `Authorization` com o valor da chave de acesso. Sem esse cabeçalho, nenhuma chamada será autorizada.

```http
Authorization: Bearer SUA_CHAVE
```

## Revogação

A revogação de uma chave de acesso é **definitiva e irreversível**. Não existe desativação temporária: uma vez revogada, a chave não pode ser reativada.

Revogue a chave pelo dashboard. A partir daí, toda requisição feita com ela é bloqueada permanentemente. A chave continua aparecendo na listagem com status `revoked` — para fins de auditoria — mas não volta a funcionar.

Se você precisa de uma pausa temporária no acesso, o caminho é **gerar uma nova chave e revogar a antiga** (rotação). Como não há limite de chaves por conta, você pode criar a nova, migrar suas integrações e só então revogar a anterior — sem janela de indisponibilidade.

## Permissão

Não é possível limitar uma chave de acesso a um número específico: dentro dos escopos que ela carrega (veja [Escopos](#escopos)), toda chave alcança o número principal da conta e todos os números extras associados.

Caso precise restringir o acesso a determinados números, é necessário criar contas separadas para cada número desejado. Essa limitação existe para evitar uso indevido do Turbo Notify.

## Escopos

Cada chave carrega **escopos de permissão** que determinam quais operações ela pode chamar — por exemplo `messages:send`, `messages:read`, `numbers:read`, `numbers:write`, `events:read`, `contacts:read`, `groups:read` e `organizations:read`. A API distingue dois erros `403`:

- `not_authorized` — a chave está ausente, inválida ou revogada.
- `insufficient_scope` — a chave é válida, mas não possui o escopo exigido pelo endpoint. A resposta inclui o campo `required_scope` com o escopo necessário:

```json
{
  "error": "insufficient_scope",
  "required_scope": "messages:send"
}
```

A tabela de erros de cada endpoint indica o escopo exigido pela operação.

## Relacionado

- [Início rápido](/general/getting-started/) — primeiro envio usando a chave.
- [Organização](/organizations/overview/) — endpoint que exige o escopo `organizations:read`.
